# Exportunity Industrial OS: Production Blockers

## P0: Contain Before Any Industrial Outreach

| Blocker | Status after this change | Required evidence before activation |
| --- | --- | --- |
| Gold-oriented legacy response path could answer an Exportunity inbound message | Contained in code; pending deployment verification | An Exportunity inbound test must create a work order without emitting an automatic reply. |
| Legacy Meta WhatsApp workflow could run retail/gold commands for Exportunity | Contained in code; pending deployment verification | An Exportunity inbound event must remain visible in the inbox without an automated outbound send. |
| Legacy Exportunity agent-economy seed could create predictable credentials, wildcard permissions, and synthetic activity | Retired in code; production read-only audit complete | The 2026-08-23 repeatable-read audit found zero directly marked rows for `exportunity_agent_economy_seed_v1`. Keep the retired seed path absent and never alter a real user password through a seed. |
| Unknown production data contamination from legacy synthetic agent activity | Read-only audit complete; no direct seed marker found | The production audit covered all 10 expected tables and returned `databaseChanged: false`, zero staff/territory/agent candidates, and zero marked wallets, tasks, token usage, cron entries, or audit logs. Any later broader identity cleanup still requires a separately reviewed remediation plan and fresh backup. |
| Unified communication decision and enforcement | Implemented in source; deployment verification open | The canonical policy returns `ALLOW_AUTO_SEND`, `REQUIRE_APPROVAL`, or `BLOCK` and is rechecked at action creation, approval, worker execution, and the email/Twilio/voice provider boundaries. Verify the deployed receipts and fail-closed behavior. |
| Provider accounts, callbacks, secrets, and verified senders | Open release evidence | Confirm the intended Google, Meta, SMTP, and Twilio accounts in their private consoles; register exact callbacks; place secrets in the deployment secret manager; verify sender/caller ownership; and retain controlled sandbox/delivery receipts. No bulk or live customer/supplier outreach is an acceptable activation test. |

No WhatsApp, SMS, email, or voice outreach campaign may be enabled for Exportunity while any P0 item remains open.

## P1: Evidence-Backed Industrial Operating Loop

1. **Open:** Attach and review the actual GDIZ fast-start CSV/JSON/ZIP. Do not derive factory records from the pasted narrative.
2. **Implemented in source:** Canonical factory, requirement, supplier, RFQ, supplier quote, commercial offer, customer quote, order, payment, and procurement-handoff records.
3. **Implemented in source:** Requirements reuse the existing tenant contact/CRM and task foundations instead of creating competing lead or agent systems.
4. **Foundation implemented; real inputs open:** Source registry, field-level provenance, verification/publication states, coverage cells, and strict country precision exist. Real source imports remain approval-gated.
5. **Implemented in source; live-provider verification open:** Requirements feed controlled supplier matching, RFQ drafts, approved quotations, orders, server-authoritative payment, and a governed procurement-release task.
6. **Implemented in source:** Public trade intelligence returns only explicitly published verified entities and facts. Empty coverage remains empty.
7. **Implemented in source; release/data-gated:** Registered-source snapshots, deterministic comparisons, regulatory review tasks, requirement-impact matches, and withheld alerts are persisted and visible. No crawler, scheduler, source corpus, automatic publication, or alert delivery was activated.
8. **Open release action:** Apply and verify migrations `20270406` through `20270423` on the intended database. For `20270410`, verify tenant/order and tracking uniqueness, immutable event idempotency/sequence constraints, optional delivery/action foreign keys, and the customer/private projection boundary. For `20270411`, verify editorial transitions, immutable revision/version uniqueness, citation/source scope, and the draft/private versus published/public boundary. For `20270412`, verify review/attachment tenant uniqueness, foreign keys, state enums, immutable events, and the approved selected-field apply audit. For `20270413`, verify one profile per tenant territory, one active activation, activation idempotency, paused zero-budget work items, source/media ownership, rights grant scope/expiry, and immutable rights events. For `20270414`, verify publication idempotency, exact states, non-secret connection references, tenant ownership, manual-not-published enforcement, provider-confirmation enforcement, and immutable attempt events. For `20270415`, verify social-event idempotency, verified-evidence constraints, canonical inbox/CRM/task foreign keys, exact classifications, and cross-tenant denial. For `20270416`, verify account tenant ownership, all hierarchical caps, internal approval constraints, paid-rights links, provider-confirmed ACTIVE enforcement, spend-ledger transaction uniqueness, conversion idempotency, and attribution bounds. For `20270417`, verify candidate/partner truth, exact tenant coverage, verified credential exclusion, quote amount/validity/evidence, one selected quote, paid-order fulfillment bindings, human booking authority, provider-submission/provider-confirmation truth, signed receipts, incident restrictions, and all three optional fulfillment carrier foreign keys. For `20270418`, verify commerce-only campaign constraints, live-readiness evidence, tier range/currency truth, exact canonical order/payment binding, paid-threshold derivation, event idempotency, production allocation/quantity gates, provider-confirmed carrier handoff, full delivery, balanced settlement/refund exposure, and provider-submission/provider-confirmation truth. For `20270419`, verify interview/project/render idempotency, exact consent/fact/status vocabularies, tenant-owned source/right/contact/media relationships, immutable events, asset provenance, credential exclusion, version uniqueness, and render submission/success truth constraints. For `20270420`, verify canonical tenant/platform/account target uniqueness. For `20270421`, verify signed credential-free receipt constraints, stable deduplication, nullable pre-resolution tenancy, exact resolution states, retention timestamps, target binding before `ingested`, and cross-tenant ambiguity quarantine. For `20270422`, verify that the existing `territory_kpis` table—not a parallel scorecard—is extended, legacy rows remain evidence-unknown, raw metric bounds hold, source window/currency/evidence/idempotency bindings fail closed, credentials are excluded, and derived rates never treat missing denominators as zero. For `20270423`, verify that the existing conversion/attribution ledgers—not a parallel commerce ledger—are extended, legacy events remain unbound, one verified binding per order is enforced, exact tenant/order/payment/amount/currency/delivery/provider-confirmed source/provenance/right checks fail closed, evidence is credential-free, and projection checksums prevent stale scorecard writes.
9. **Open release action:** Complete a provider-sandbox/live callback test with the configured Exportunity Flutterwave account; do not perform a live charge as part of an ordinary code verification.
10. **Open release action:** Exercise the paid-order fulfillment state machine in a non-production tenant, including inspection evidence, freight reference, customs/last-mile conditions, delivery proof, replayed idempotency keys, and an attempted cross-tenant delivery link. Do not create a delivery-agent offer or contact an external provider during this verification.
11. **Implemented in source; provider acceptance open:** Supported requirement documents persist deterministic text and explicit review states. Images now have a foreground, feature-gated strict-schema proposal path plus human edit, approval/rejection, and selected-field apply controls. Scanned-PDF OCR and CAD remain manual/technical review paths. Apply migration `20270412`, complete provider/data-governance review, and run the non-sensitive deployed acceptance matrix in `05-attachment-intelligence-review.md` before claiming live image understanding.
12. **Implemented in source; provider release open:** Exportunity outbound channels require the global kill switch, a channel-specific release flag, a verified sender/caller flag, recipient governance, business-hour/quota checks, and a matching visible action receipt. Google Workspace connections remain read-only; the optional YouTube connection requests upload/read-only scopes, and Meta connections request the Page/Instagram publishing scopes. Additional Meta comment/message scopes are requested only when the disabled-by-default inbound webhook feature is deliberately released. Provider app review, business-target verification, live callbacks, and controlled receipts remain open. Voice remains fail-closed until a canonical voice action/worker path exists.
13. **Implemented in source; production legacy audit open:** All new CMS publication transitions fail closed without source provenance and a current rights/consent grant. Existing published media is intentionally not mass-unpublished by a source migration; inventory it read-only, collect evidence or restrict/remove it through an approved remediation plan, and retain takedown/revocation evidence.
14. **Implemented in source; provider/browser release open:** Social publication preparation exposes safe connection readiness and complete manual packages with exact states; a manual package can never become `PUBLISHED`. Mindbase binds signed integration state to the correct provider callback, records only provider-evidenced grants, preserves missing/declined/unknown scope truth, exchanges Meta's short-lived user credential for the documented long-lived credential, and blocks legacy or unverified evidence. A disabled-by-default official Meta slice can create one confirmed Facebook Page image/link post or Instagram single-JPEG post, resolves the Page credential only transiently, requires provider read-back before `PUBLISHED`, leaves unfinished containers in `PROCESSING`, and never automatically retries. Twilio has a separate read-only account check that masks identifiers and cannot send a message. Complete the private Google, Meta, and Twilio setup, register only the released entries in the exact callback manifest in `docs/territory-media-commerce/03-provider-authorization-readiness.md`, verify tenant-owned targets/scopes/tasks, deploy and migrate, retain controlled receipts, and keep `FEATURE_META_SOCIAL_PUBLICATION_ADAPTER=false` until a separately approved release test.
15. **Implemented in source; provider adapter release open:** Social comments and DMs can enter the canonical Team Inbox/CRM only through a tenant-admin or trusted adapter path with bounded verified evidence. A shared fail-closed Meta challenge/raw-body HMAC primitive secures the dormant Meta WhatsApp aliases; the feature-gated Facebook/Instagram route parses documented text comments/DMs, deduplicates provider events, resolves exactly one eligible tenant target, durably quarantines unsupported/unmatched/ambiguous/ineligible/failed events, and exposes explicit foreground reconciliation without an automatic retry loop. Canonical Twilio callbacks retain provider-specific verification. Apply `20270421`, complete Meta app review, reconnect for the conditionally requested inbound scopes, select and verify targets, configure the callback/secret/feature flag, and retain a controlled signed receipt before registering or enabling the Graph callback. Provider pollers remain absent. No social reply can leave the system until an outbound adapter plus approved facts, prices where relevant, policies, tone, and human approval all pass.
16. **Implemented in source; live ad account/billing release open:** Advertising governance is preparation-only. Before any provider submission, verify the business-owned account and tenant billing in the private console, exact scoped permissions, account health/restrictions, app review, active hierarchy of caps, current orderability/stock/delivery/landing/tracking evidence, paid rights, policy approval, margin/CAC, stopping rules, and an unexpired approved authorization. There is no source path that spends money today.
17. **Implemented in source; carrier adapter/release evidence open:** The Carrier Network workspace can record candidates, verification, exact coverage, a non-secret connection reference, structured quote receipts, selection, internal booking authority, and incidents. It cannot request a provider quote, create/cancel a booking, or project tracking. No adapter is registered. Before enabling one, verify the tenant-owned carrier account, capabilities, callback/signature setup, restrictions, sandbox receipt idempotency, rollback plan, and a separate action-time approval. Never represent `submission_ready` or `approved_submission_ready` as booked or provider-confirmed.
18. **Implemented in source; group-commerce production/provider release open:** The Producer Exchange and admin workspace can prepare a commerce-only campaign, record non-binding interest, bind an exact provider-reconciled paid industrial order, allocate a production batch, retain production/inspection/carrier/delivery evidence, publish local updates, and prepare a balanced settlement plan. They cannot create a second checkout, reserve inventory, start factory production, book a carrier, submit settlement, release funds, or execute a refund. Apply and verify `20270418`; then prove the canonical payment, carrier, refund, and settlement receipt paths in a non-production tenant before a separately authorized bounded pilot.
19. **Implemented in source; interview/media provider and privacy release open:** The tenant-admin Interview & Media Studio records consent-aware intake, progressive claims with exact fact tags, paused zero-budget evidence work, source/right-linked projects, provenance assets, edit versions, approval hashes, and render manifests. It cannot contact or record an interviewee, upload/transcribe/render through a provider, publish, advertise, or spend. Apply and verify `20270419`, complete privacy/retention/AI-disclosure and object-storage controls, then add separately approved transcription/render/upload adapters with controlled sandbox receipts. See `docs/territory-media-commerce/04-interview-and-studio-workflow.md`.
20. **Implemented in source; semantic provider acceptance open:** Commercial intake remains deterministic-first and spends no semantic call for a recognized product. An explicit commercial request whose product is still unknown may make one visible foreground OpenAI Responses request behind global AI consent and a configured key. The response is strict-schema, stores no action mode, needs at least 0.82 confidence, and is validated before deterministic qualification recomputes all missing fields and the next mode. Run a controlled non-production acceptance set with unfamiliar raw materials, machinery, parts, services, ambiguous language, refusals, malformed output, provider failure, and prompt-injection text before claiming the semantic path live. No provider test may create a supplier contact, order, payment, publication, or other external action.
21. **Implemented in source; migration and real evidence open:** The existing `territory_kpis` table now accepts nullable, field-evidenced media-to-commerce metrics through a confirmed foreground `TERRITORY_SCORECARD_RECORD` Action. The Territory Hub derives conversion, payment, delivery, on-time, dispute, refund, repeat-purchase, CAC, and cost-per-order indicators only from stored numerators/denominators; missing values remain unknown. Existing territory-bound advertising envelopes are visible through the same operating response without creating a second budget or approving spend. Apply `20270422`, validate legacy rows and constraints, then record a non-sensitive canonical-ledger bundle in a non-production tenant before treating a scorecard as operational.
22. **Implemented in source; migration and non-production evidence open:** `COMMERCE_ATTRIBUTION_RECONCILE` now binds one completed industrial order to its exact succeeded payment, delivered fulfillment plan, and one provider-confirmed, current-rights ad or organic publication in the existing conversion/attribution ledgers. The Territory Hub recomputes a read-only checksum-bound projection from verified attribution, reconciled media spend, group campaigns, social leads, rights, and publications, and keeps unsupported metrics unknown. Apply `20270423`; attempt cross-tenant, wrong-payment, amount/currency drift, undelivered, unconfirmed-source, expired/revoked-rights, duplicate-order, reused-idempotency, stale-checksum, and credential-like evidence failures in a non-production tenant before recording a real scorecard. No provider call, publication, spend, message, or background job is part of this acceptance test.

## P2: Governed Industrial Run

The future Go control must create a durable industrial operating run with explicit states:

`STOPPED -> PREFLIGHT -> DISCOVERY -> ENRICHMENT -> QUALIFICATION -> OUTREACH READY -> ACTIVE -> DRAINING -> STOPPED`

It must be able to pause, fail safely, and emergency-stop. It is not a background agent conversation. Each stage must create visible audit evidence, with no external outreach, pricing commitment, payment, or contract action outside the policy gate.

## Verification Required for the P0 Release

- The focused safety suite and direct compilation of the touched server modules pass.
- The full repository route verification and TypeScript check pass locally with the repository's 4 GB TypeScript heap setting.
- The targeted tenant, industrial intake, and inbound-messaging policy tests pass.
- The live health SHA equals the deployed commit.
- No outbound Twilio or WhatsApp action is sent as part of verification.
- Provider secrets are entered only in the private console or deployment secret manager and never pasted into chat or committed.
- See `docs/industrial-os/03-outbound-communications-policy.md` for the activation and rollback checklist.
